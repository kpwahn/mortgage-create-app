const drawerWidth = 240;

const styles = theme => ({
  MaterialDrawer: {
    position: 'relative',
    width: drawerWidth,
    height: '100%'
  },
  MaterialDrawerDocked: {
    height: '100%'
  }
});

export default styles;
